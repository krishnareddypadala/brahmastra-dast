# BRAHMASTRA — Scanner Dependencies
# Install: pip install -r requirements.txt

# ── Core HTTP ────────────────────────────────────────────────────
httpx[http2]>=0.27.0         # Async HTTP client with HTTP/2 support
httpx-ntlm>=1.4.0            # NTLM auth for httpx

# ── CLI ──────────────────────────────────────────────────────────
click>=8.1.7
rich>=13.7.0                 # Pretty terminal output

# ── API parsing ──────────────────────────────────────────────────
pyyaml>=6.0.1                # YAML (OpenAPI specs)
jsonschema>=4.21.0
openapi-spec-validator>=0.7.1

# ── Database ─────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.25
alembic>=1.13.1
asyncpg>=0.29.0              # PostgreSQL async driver
aiosqlite>=0.20.0            # SQLite async driver

# ── Auth ─────────────────────────────────────────────────────────
pyotp>=2.9.0                 # TOTP (MFA)
python-jose[cryptography]>=3.3.0  # JWT
boto3>=1.34.0                # AWS Signature v4
cryptography>=42.0.0         # mTLS

# ── Browser automation (for form/SAML/OAuth PKCE auth) ──────────
playwright>=1.42.0

# ── Reporting ───────────────────────────────────────────────────
weasyprint>=62.3             # HTML → PDF
jinja2>=3.1.3                # Report templates
sarif-tools>=3.0.0           # SARIF 2.1

# ── Security tools integration ───────────────────────────────────
# (nuclei, httpx, ffuf are external Go binaries — not pip)

# ── API server ──────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# ── Utilities ───────────────────────────────────────────────────
python-multipart>=0.0.9
aiofiles>=23.2.1
